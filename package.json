{
  "name": "crossword",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build && node scripts/prepare-dist.mjs && node scripts/generate-service-worker.mjs",
    "preview": "vite preview",
    "verify:build": "node scripts/verify-build.mjs"
  },
  "devDependencies": {
    "vite": "^8.3.0"
  }
}
